# Synapse v1.2.1

Release date: 2026-02-15

## Fixed

- Deep Research mode now attempts `--search` first, then falls back automatically if unsupported by local Codex CLI.
- Retry strategy optimized to stop fast on hard errors (unsupported model/args).

## Changed

- README, packaging scripts, and Homebrew cask version synced to `v1.2.1`.

## QA

- `swift build` passed.
- `swift test --filter IntentEngineTests` passed.

## Release Assets

- `Synapse-v1.2.1-macos-arm64.zip`
- `Info.plist`
- `SHA256SUMS.txt`
